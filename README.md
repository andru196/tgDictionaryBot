# tgDictionaryBot

Личный голосовой дневник в Telegram, который разбирается локальной LLM и превращается в один красивый HTML-файл.

Наговариваешь в свой приватный канал что попало — что съел, что заболело, какая мысль пришла, что надо загуглить. Потом запускаешь анализ: локальный Whisper расшифровывает голосовые, локальная модель в LM Studio раскладывает речь по категориям, детерминированный код считает корреляции «еда → симптом» за любой период, и на выходе получается отчёт.

**Ничего не уходит наружу.** LLM локальная, распознавание локальное, база локальная. Единственное соединение с интернетом — за собственными сообщениями в Telegram.

## Быстрый старт в Docker

```bash
cp .env.example .env && cp docker/appsettings.Local.example.json docker/appsettings.Local.json
```

Заполнить `.env` (`api_id`/`api_hash` с my.telegram.org) и вписать свой канал в `docker/appsettings.Local.json`. Оба файла в `.gitignore` и в `.dockerignore` — ни в репозиторий, ни в образ они не попадают.

Прогон на выдуманных данных, без Telegram и без учётной записи — нужна только запущенная LM Studio:

```bash
docker compose run --rm diary sync --source file:samples/messages.jsonl
```

```bash
docker compose run --rm diary extract
```

```bash
docker compose run --rm diary report --period month --compare
```

Отчёт появится в `./reports/`, база и голосовые — в `./data/`: тома переживают пересборку образа.

Именно `run`, а не `up`: команды разовые, а первый вход в Telegram спросит код из сообщения — нужен терминал.

Модель по умолчанию берётся с хоста через `host.docker.internal`. Если хочется полной автономности, профиль `bundled` поднимает Ollama рядом:

```bash
docker compose --profile bundled run --rm diary-bundled extract
```

Тесты прогоняются в том же образе, где собирается рабочий:

```bash
docker compose build tests
```

## Без Docker

```bash
dotnet run --project src/Diary.Cli -- sync --source file:samples/messages.jsonl
```

Конфигурация — `appsettings.Local.json` рядом с исполняемым файлом, пример в [docs/appsettings.example.json](docs/appsettings.example.json).

## Команды

```bash
diary sync                                    # забрать новое из каналов
diary transcribe                              # расшифровать голосовые
diary extract                                 # разобрать моделью в записи
diary answer --period month                   # ответить на накопленные вопросы
diary run --report --open                     # всё сразу
diary report --from 2026-01-01 --to 2026-12-31 --group-by month
diary eval --set fixtures/golden-ru.jsonl     # измерить качество разбора
diary eval --model qwen/qwen3.5-9b --json     # сравнить модели между собой
diary status                                  # что накоплено и что в карантине
diary retention --subject me --mode delete --dry-run
diary reprocess --from-state Transcribed --since 2026-06-01
```

## Документация

| Документ | О чём |
|---|---|
| **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** | топология, слои и модули, доменная модель, хранение, пайплайн, связывание «еда → симптом», статистика за произвольный период |
| **[docs/EXTENSIBILITY.md](docs/EXTENSIBILITY.md)** | как добавить новый вид записей, не трогая существующий код |
| **[docs/MODELS.md](docs/MODELS.md)** | выбор локальной модели, роли по шагам, аудио |
| **[docs/MULTI-SUBJECT.md](docs/MULTI-SUBJECT.md)** | несколько наблюдаемых людей, маршрутизация по отправителю, изоляция данных |
| **[docs/report-sample.html](docs/report-sample.html)** | эталон вёрстки отчёта с выдуманными данными |

## Четыре решения, на которых держится остальное

**MTProto, а не бот.** Bot API хранит непрочитанные апдейты 24 часа — с ним «включу раз в год» было невозможно. Личный аккаунт читает всю историю и качает медиа любой давности, так что always-on демон не нужен: всё живёт в одном консольном приложении, которое запускается когда захочется.

**LLM только извлекает структуру, считает обычный код.** Локальная модель невоспроизводима между запусками и плохо считает. Корреляции, окна экспозиции и точный тест Фишера живут в детерминированном коде и покрыты тестами — статистика обязана быть одинаковой при повторном прогоне.

**Сырьё отдельно от интерпретации.** Сообщения и транскрипты хранятся навсегда, рядом с каждой извлечённой записью пишется модель и версия промпта. Вышла модель получше — `diary reprocess`, и вся история разобралась заново.

**Субъект — это скоуп, а не параметр.** Людей может быть несколько, у каждого своя база. Забытый `WHERE SubjectId = …` — баг, который не падает и выглядит как правдоподобные цифры, поэтому колонки `SubjectId` просто нет: у каждого свой файл БД. Маршрутизация — по id отправителя, неопознанные уходят в карантин.

## Измерение качества

Публичные бенчмарки меряют что угодно, только не разговорный русский про изжогу, поэтому «какая модель лучше» — вопрос измеряемый:

```bash
diary eval --set fixtures/golden-ru.jsonl
```

Прогон на 20 размеченных примерах даёт точность сегментации, F1 по продуктам и свойствам пищи, точность вида симптома, ошибку шкалы тяжести, долю невалидных ответов и цену прогона — плюс список случаев, где разошлось. Тем же харнессом ловятся регрессии после правки промпта.

Свой набор собирается за час: взять 50–100 реальных сообщений, прогнать самой сильной доступной моделью и поправить разметку руками. Подробнее — [docs/MODELS.md](docs/MODELS.md).

## Тесты

```bash
powershell -File test.ps1
```

109 тестов: доменная логика, маршрутизация по отправителям, валидация конфигурации, математика статистики (lift, точный тест Фишера, границы периодов, калибровка окон), разбор OGG вплоть до сквозного round-trip через настоящий кодек Opus, чистка ответов модели и рендер отчёта. Всё офлайн, без сети и без модели.

## Что пока не реализовано

- **Проекции для статистики** (`gi_meal_items`, `gi_symptoms` из плана). Запросы идут по таблице `entries` с индексом на время — для личного дневника этого хватает с запасом; проекции нужны при десятках тысяч записей.
- **Миграции EF Core.** Схема создаётся через `EnsureCreated`; перевести на миграции надо перед первым изменением схемы, иначе накопленные базы придётся пересоздавать.
- **Telegram-адаптер написан, но не проверен вживую** — нужны `api_id`/`api_hash`. Файловый источник прогнан полностью.
- **Нативный аудио-вход написан, но не проверен** — нужна модель с аудио-входом в LM Studio. Каскад через Whisper проверен на разборе OGG, но не на настоящих весах Whisper: их надо скачать в `models/`.

## Стек

.NET 10 · WTelegramClient (MTProto) · Whisper.net + собственный разбор OGG/Opus · LM Studio по HTTP · EF Core + SQLite · HTML-отчёт без единой внешней зависимости

## Лицензия

MIT

