# Личное — не должно попадать в образ ни при каких обстоятельствах.
.env
**/appsettings.Local.json
**/appsettings.*.Local.json
docker/appsettings.Local.json
data/
reports/
models/
**/*.session
**/*.db
**/*.ogg

# Результаты сборки хоста: в контейнере всё собирается заново.
**/bin/
**/obj/
artifacts/
TestResults/

# Не влияет на сборку.
# Внимание: маска только для корня — промпты модулей тоже .md, и они встроенные
# ресурсы. Исключить их значит получить образ, который падает на первом же разборе.
.git/
.github/
.vs/
.vscode/
.idea/
docs/
/*.md
